Objetivo: practicar listas enlazadas y ArrayList juntos.

Contexto:

Quieres simular un sistema básico de biblioteca.

Requisitos:

Crea una lista enlazada para los libros disponibles (cada nodo representa un libro).

Datos: título, autor, año, disponible (boolean).

Crea un ArrayList para almacenar los libros prestados.

Implementa las siguientes operaciones:

Agregar libro → se agrega al final de la lista enlazada.

Ver libros → muestra todos los libros con su estado.

Prestar libro → pasa un libro de la lista enlazada al ArrayList.

Devolver libro → lo quita del ArrayList y lo agrega otra vez a la lista enlazada.

Buscar libro por título o autor. 


EJERCICIO DEFINITIVO 
Ejercicio: Sistema de Reservas de Clases en un Gimnasio
Contexto:

Un gimnasio quiere controlar las reservas de clases que hacen los clientes.
Cada clase tiene un número limitado de cupos y cada cliente puede reservar una clase mientras haya espacio.

📋 Estructura general

Lista enlazada → Guardará las reservas de cada cliente (cada nodo representa una reserva activa).

ArrayList → Guardará todas las clases disponibles del gimnasio (y sus cupos).

⚙️ Detalles del sistema

Cada clase tiene:

Nombre (por ejemplo, “Zumba”, “Crossfit”, “Yoga”).

Instructor.

Capacidad máxima.

Número actual de reservaciones.

Cada cliente (en la lista enlazada) tiene:

Nombre del cliente.

Clase que reservó.

Estado (activa o cancelada).

🔧 Operaciones que debe permitir el programa

Agregar clases al gimnasio (se almacenan en el ArrayList).

Ver clases disponibles (mostrar solo las que aún tengan cupos).

Registrar una reserva:

Se busca la clase en el ArrayList.

Si hay espacio, se crea un nodo en la lista enlazada con la información del cliente.

Se aumenta el contador de reservaciones en la clase.

Ver todas las reservas activas (recorriendo la lista enlazada).

Cancelar una reserva:

Se busca al cliente en la lista enlazada.

Se elimina el nodo correspondiente y se reduce el contador de la clase en el ArrayList.

Mostrar clases con más reservas (analizando los datos del ArrayList).

🎯 Objetivo

Practicar cómo:

Recorrer una lista enlazada para buscar, eliminar o mostrar datos dinámicos (las reservas).

Modificar datos dentro de un ArrayList (las clases y sus cupos).

Mantener la sincronización entre ambas estructuras.


///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

Ejercicio — Sistema de Talleres Universitarios
🏫 Contexto

La universidad desea controlar los talleres extracurriculares (por ejemplo: Robótica, Programación, Fotografía, Pintura).
Cada taller tiene un límite de cupos, y los estudiantes pueden inscribirse mientras haya espacio disponible.

🧱 Estructura que debes usar

ArrayList → Para almacenar todos los talleres disponibles.
Cada objeto debe guardar:

Nombre del taller

Profesor responsable

Capacidad máxima

Cupos ocupados

Lista enlazada manual (con nodos) → Para guardar las inscripciones de estudiantes.
Cada nodo representa un estudiante inscrito en un taller.

🧮 El sistema debe permitir

Agregar talleres al ArrayList.

Ver talleres disponibles (solo los que aún tienen cupos).

Registrar un estudiante en un taller

Se busca el taller en el ArrayList.

Si hay cupo, se crea un nodo en la lista enlazada con:

Nombre del estudiante

Taller inscrito

Se incrementa el contador de cupos ocupados.

Mostrar todos los estudiantes inscritos y en qué taller están.

Cancelar inscripción de un estudiante.

Elimina el nodo correspondiente en la lista enlazada.

Reduce el cupo ocupado del taller en el ArrayList.

Mostrar el taller con más inscritos


//////////////////////////////////////////////////////////////////////////////////////////////

🧩 Problema: Sistema de préstamos de libros en una biblioteca

Una biblioteca quiere gestionar los préstamos de libros a los usuarios.
Cada usuario puede tener uno o varios libros prestados, 
pero cada libro solo puede estar prestado a un usuario a la vez.

El sistema debe permitir:

Registrar nuevos libros en la biblioteca.

Registrar nuevos usuarios.

Permitir que un usuario solicite un libro (si está disponible).

Permitir que un usuario devuelva un libro (liberando el préstamo).

Mostrar la lista de usuarios con los libros que actualmente tienen prestados.

Mostrar los libros que aún están disponibles para préstamo.

📘 Pistas lógicas (no técnicas):

Piensa cómo relacionar a los usuarios con los libros que tienen prestados.

Piensa qué ocurre con un libro cuando pasa de “disponible” a “prestado”.

Considera qué debe pasar si un usuario devuelve su último libro.

Reflexiona cómo evitar que un mismo libro se preste a dos personas al mismo tiempo.

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

Problema: Sistema de pedidos de restaurante

Un restaurante necesita un programa para gestionar los pedidos de los clientes del día.
Cada pedido se agrega dinámicamente a una lista enlazada.

Tu programa debe permitir lo siguiente:

Agregar un pedido con:

nombre del cliente,

nombre del platillo,

precio del platillo.

Mostrar todos los pedidos actuales.

Calcular el total de ventas del día sumando todos los precios.

Marcar un pedido como entregado (eliminarlo de la lista).

Buscar un pedido por nombre de cliente y mostrar su platillo y precio.

Cancelar todos los pedidos (vaciar la lista).

🎯 Retos opcionales:

Mostrar un mensaje si no hay pedidos.

Evitar duplicados de cliente.

Mostrar la cantidad total de pedidos.



